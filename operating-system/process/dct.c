#include <stddef.h>
#include <stdint.h>
#include <matrix.h>

/*
 * Cette fonction effectue une transformée de cosinus discrète.
 *
 * @param matrix La matrice 2D à tranformer.
 * @param axis   L'axe de transformation (0 ou 1).
 *
 * @return La matrice 2D résultantes (allouée dynamiquement).
 */
struct matrix* dct(struct matrix* matrix, unsigned axis) {
    /* TODO: écrire votre code ici */
    return NULL;
}
